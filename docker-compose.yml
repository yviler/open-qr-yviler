services:
  app:
    build: .
    ports:
      - "5001:5001"
    environment:
      POS_BASE_URL: http://your-pos-host:5000
      QR_TOKEN: ""
      QR_LANG: en
      PORT: "5001"
    restart: unless-stopped
